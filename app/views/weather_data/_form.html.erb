<%= form_with(model: weather_datum, local: true) do |form| %>
  <% if weather_datum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(weather_datum.errors.count, "error") %> prohibited this weather_datum from being saved:</h2>

      <ul>
      <% weather_datum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :zipcode %>
    <%= form.text_field :zipcode, id: :weather_datum_zipcode %>
  </div>

  <div class="actions">
    <%= form.submit "Fetch weather data" %>
  </div>
<% end %>
